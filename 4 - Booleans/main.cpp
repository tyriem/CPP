// C++ Project 4 - Booleans

//Bring in the iostream library for print functionality
#include <iostream>

//Initiate the main function
    int main() {

//When declaring a variable we declare the type then the name then assign it value (type name = value;)
//NB: In the case below we use the type boolean (bool)
bool isOn = true;

        std::cout << isOn;

//After the initial declaration of the bool we can change its state after
isOn = false;
//NB: When using a print statement we can add an end line (<< std::endl) to end the print statement and line
        std::cout << isOn << std::endl;

    }
